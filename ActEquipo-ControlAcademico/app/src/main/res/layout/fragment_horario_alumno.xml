<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F7FA"
    android:padding="24dp">

    <!-- Título -->
    <TextView
        android:id="@+id/tvTituloHorarioAlumno"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Mi Horario"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#1565C0"
        android:layout_marginBottom="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tvSubtituloHorario"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Materias y horarios del semestre"
        android:textSize="13sp"
        android:textColor="#666666"
        android:layout_marginBottom="20dp"
        app:layout_constraintTop_toBottomOf="@id/tvTituloHorarioAlumno"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ProgressBar -->
    <ProgressBar
        android:id="@+id/progressHorarioAlumno"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tvSubtituloHorario"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Mensaje sin materias -->
    <TextView
        android:id="@+id/tvSinMateriasAlumno"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="No estás inscrito en ninguna materia."
        android:textSize="14sp"
        android:textColor="#888888"
        android:gravity="center"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tvSubtituloHorario"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Lista del horario -->
    <ListView
        android:id="@+id/listViewHorarioAlumno"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:divider="#E0E0E0"
        android:dividerHeight="1dp"
        app:layout_constraintTop_toBottomOf="@id/tvSubtituloHorario"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>